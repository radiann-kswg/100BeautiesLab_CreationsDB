{
  "name": "100beautieslab_creationsdb",
  "private": true,
  "version": "0.1.0",
  "description": "Test setup and data validation for 100BeautiesLab_CreationsDB",
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "dict:plan-enums": "node tools/extract-enum-lists-to-dictionaries.mjs",
    "dict:export-enums": "node tools/extract-enum-lists-to-dictionaries.mjs --write",
    "dict:export-enums:prune": "node tools/extract-enum-lists-to-dictionaries.mjs --write --prune"
  },
  "devDependencies": {
    "glob": "^11.1.0",
    "jsdom": "^26.1.0",
    "playwright": "^1.60.0",
    "vitest": "^4.1.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
