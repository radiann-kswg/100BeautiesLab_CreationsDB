# 2026-04-21 progress: multi-index-display

- 目的: `#Index` が object 形式で複数要素を持つ作品で、キャラシートが 1 要素だけに固定されず、`$display.index` で一覧/詳細/直リンクの表示対象を制御できるようにする。
- 変更点の要約:
  - `pages/characters.js` の index helper を複数要素対応へ整理し、object 形式 `#Index` の値収集を一覧/詳細/直リンク/一致判定で共用化
  - 既定値を「一覧/直リンクは主要要素」「詳細/値表示は全要素」とし、各子要素の `$display.index` で `list/detail/value/link/priority/order` を上書き可能にした
  - `docs/db-update-guidelines.md`、`docs/implementation-playbook.md`、`.github/copilot-instructions.md`、`CHANGELOG.md` へ運用ルールを同期
- 影響範囲（編集予定を含む）:
  - `pages/characters.js`
  - `docs/db-update-guidelines.md`
  - `docs/implementation-playbook.md`
  - `.github/copilot-instructions.md`
  - `CHANGELOG.md`
- 未完了タスク:
  - HTTP サーバー上での目視確認（複数 index pill/chip の見え方）
- 検証:
  - `tests/pages.characters.syntax.test.js`: pass
  - `tests/docs.links.test.js`: pass
  - `get_errors(pages/characters.js)`: 問題なし
  - 追補: ブラウザ実行時に `collectIndexEntries()` / `getIndexSubDefDisplayConfig()` から参照する `isPlainObject` がトップレベル未定義で `ReferenceError` になっていたため、`pages/characters.js` 上位へ共通 helper を追加して復旧
- 参考リンク:
  - `2026-03-31_remaining-task.md`
  - `pages/characters.js`