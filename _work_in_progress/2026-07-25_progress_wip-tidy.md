# 進捗ログの棚卸し（2026-07-25 / `develop`）

## 目的

User 依頼「進捗ログを整理してほしい。あわせて**今後どのように対応すべきか**を明文化し、
**放置されている重大タスクが無いか**に注意して作業してほしい」への対応。

前回（2026-07-22）の棚卸し後、`_work_in_progress/` 直下は 24 件（+README）。
今回は「件数を減らす仕分け」よりも **「取りこぼしている重要事項の発見」** に重心を置き、
直下 20 件すべての未完了節を読み合わせたうえで、実地で裏取りした。

## 方針（前回踏襲 + 今回の追加）

- 退避判断は**書面上の「完了/未実施」記載を鵜呑みにせず、実際に確認して裏取りする**（前回踏襲）。
- **今回の追加**: 「未完了」と書かれている項目についても、**本当にまだ壊れているのか**を実測する。
  書面が古くなって「解決済みなのに残っている」ケースと、逆に「軽く書かれているが実害が出ている」
  ケースの**両方**を洗う。
- コード（`pages/` `lib/` `data/` `tests/`）は本棚卸しでは**変更しない**。発見した課題は母艦へ登録して申し送る。
- 棚卸し成果は**未コミットで残す**（User の確認後にコミット指示を出す運用）。

---

## 1. 裏取り（実測）

対象 HEAD は `develop` の `b737891`（`origin/develop` と 0/0 同期）。

### 1-1. 定点観測

| 観点 | コマンド | 結果 |
| --- | --- | --- |
| テスト | `npm test` | ✅ **42 ファイル / 582 件すべて成功** |
| 指示書の生成物 | `npm run agents:check` | ✅ `0/2 件が要更新`（正典と一致） |
| データのキー順 | `npm run data:order:check` | ✅ `0/1284 レコード`（全 19 ファイル整列済み） |
| 一方向マージ | `git rev-list --left-right --count develop...origin/addon-ai-tag` | ⚠️ **1 / 108**（develop 側 1 件が未取り込み） |
| CI の実状態 | `gh run list` | ✅ AIHints / Cloudflare / Pages とも直近 run は **success** |
| 未解決 Issue | `gh issue list` | 🟡 **#13 のみ OPEN**（2026-07-21 起票） |

### 1-2. 書面と実態のズレ（3 件・いずれも「未コミット」表記が古かった）

README のトピック索引と各ログが「成果は未コミット」と記していた 3 件は、**すべて既に着地済み**だった。

| ログ | 書面 | 実測 |
| --- | --- | --- |
| `2026-07-22_progress_wip-tidy.md` | 成果は未コミット | **`dfe2273`**「進捗ログ整備＆テスト回路修正」で着地 |
| `2026-07-22_progress_agents-ssot.md` | 成果は未コミット | **`2b30754`**「GPT Codex導入」で着地 |
| `calendar-same-person-dblink.md` | Claude Desktop 側の未コミット実装 | **`a20fb7b`**「ICSカレンダー機能更新」で着地 |

あわせて `agents-ssot` の未完了項目 2 件（未追跡の `.agents/` / `addon-ai-tag` への波及）も解決済みを確認した
（`.agents/skills/localize-en-draft/SKILL.md` が追跡済み・`79cafd1` のマージで波及済み）。

### 1-3. 逆に「軽く書かれていたが実害が出ていた」もの（最重要）

`2026-07-24_progress_roleplay-prompt-formatting.md` は 3 件を「User 判断待ち・要望の範囲外のため未修正」と
記録していた。生成物 66 件を全走査したところ、**配布用の成果物に実際に出力されていた**。

| 症状 | 実測 |
| --- | --- |
| `[object Object]` | **66 件中 10 件**（体重 7 / **年齢 3** / 身長 1） |
| 句点の二重化 `。。` | 1 件（`NumberTales/DB_SemiPrimary/roleplay-prompt-100.md`） |
| `…である一方、` で文が切れる | 1 件（同上） |

**元ログの記述に誤りがあった**: 「`@Age` は `buildVars()` 側でアンラップ済み」とあるが、
`61` / `96` / `97` は `設定年齢は[object Object]歳です。` になっており、`@Age` も同じ症状が出ている。
`15` にいたっては `体重[object Object], [object Object]kg` と配列 × object の二重化まで起きている。

`node tools/build-roleplay-prompts.mjs --check` は `changed=0 unchanged=57` の冪等状態なので、
**再生成では直らない**（`render.mjs` / テンプレ側の修正が要る）。

→ 元ログへ訂正を追記し、母艦 **P4-8** へ「配布物に実害あり」として格上げ登録した。

### 1-4. 自動トリアージが同じ誤りを 2 回繰り返していた

当日の `2026-07-25_github-triage.md` §1 が、退避済みの `2026-07-22_github-triage.md` §3 と
**同じ誤った仮説**（`AI_Optout: true` の DB が列挙に混入して `patch-aihints.mjs` が exit 2 で落ちる）を
再掲し、同じワークフロー修正を提案していた。

この仮説は同日の `2026-07-25_progress_aihints-resync-ci-failure.md` で**既に否定済み**
（実際は `テスト` ステップの赤が原因で、`AI_Optout` ガードは一度も発火していない）。

→ トリアージログへ訂正注記を追加し、**「提案は適用しないこと」**を明記した。
根拠となる CI 失敗ログは退避せず現行に残置している。

**原因**: 自動トリアージは Actions 実行ログへ未接続で、Gmail 通知とワークフロー定義だけから推論している。
→ 台帳の運用メモへ「実行ログを確認するまで提案を適用しない」を明記した。

### 1-5. その他の実測

- **`docs/readme.en.md`**: 最終更新 `b37ba54`（**2026-04-30**）。`Cloudflare` の文字列が **0 件**で、
  実 API が英語圏読者へ一切案内されていない。`2026-06-24_progress_localization-rules-audit.md` の
  申し送りに書かれていたが、**約 1 か月動いていない**。→ 母艦 P4-9。
- **`$VersDef` / `$VarsDef` 表記ゆれ**: `$VersDef` を持つのは 4 作品の `DataBases/db_type.json`。
  中身は `$Def_Relations` / `$Def_EffectText` / `$Def_ThisMastersEntry` で、
  **いずれもグローバル `data/db_meta.json` の `General.$VarsDef` には存在しない**。
  `lib/data-common.js` は `$VarsDef` のみ合成する（`:361-362`）ため合成経路から漏れている。
  表示は別経路で成立しているとみられ実害は観測されていないが、「表記ゆれだけ」と断定するには
  合流経路の裏取りが要る。→ 母艦 P4-11。
- **ColorPalette と AIHints の断絶**: `develop` 側で `ColorPalette` を **94 件**投入済み（全件 5 色以上）、
  `addon-ai-tag` 側で `--resync-structural` も稼働済み。しかし**両者を繋ぐ導出モードが未実装**のため、
  AIHints の `palette_priority` は依然 **92/92 件 `null`**。→ 母艦 P7-1（後述）。

---

## 2. 退避（4 件 → `.completed/`）

**`_work_in_progress/` 直下: 24 件 → 20 件（+ 棚卸しログ本体 1 件で 21 件・+README）**

| ログ | 退避理由 |
| --- | --- |
| `2026-07-22_progress_wip-tidy.md` | 「未コミット」→ `dfe2273` で着地済み。申し送り 4 件も全て解決を確認 |
| `2026-07-22_progress_agents-ssot.md` | 「未コミット」→ `2b30754` で着地済み。`.agents/` 追跡・`addon-ai-tag` 波及とも完了 |
| `calendar-same-person-dblink.md` | 実装は `a20fb7b` で着地済み。外部反映 2 件は母艦 P4-14 へ引き継ぎ |
| `2026-07-22_github-triage.md` | `2026-07-25_github-triage.md` へ世代交代。§3 の誤りと §4 の解消を追記のうえ退避 |

4 件とも、移動後に確認結果を追記している。

**退避しなかったもの（判断の記録）**:

- `2026-07-25_progress_aihints-resync-ci-failure.md` は 🟢 完了だが、**当日のトリアージ §1 を訂正する根拠**
  であり、申し送り①（全体 `npm test` 依存）が運用上まだ効いているため現行に残置した。
- `2026-07-08` / `2026-07-13` の AIHints 提案・診断 2 件は「提案のみ・未着手」と書かれているが、
  実際には第0階〜第2階まで実装が進んでいる。**残 1 本の設計背景として参照価値がある**ため残置し、
  索引の状態表記だけを実態へ更新した。

---

## 3. 放置タスクの洗い出し（本棚卸しの主眼）

直下 20 件の未完了節をすべて読み、**「放置されると何が起きるか」**で層別した。

### 層 S — 公開物・配布物に実害が出ている

| # | 項目 | 実害 | 放置期間 |
| --- | --- | --- | --- |
| S-1 | ロールプレイプロンプトの `[object Object]` ほか | 配布用成果物 66 件中 10 件が壊れた表示のまま | 1 日（**実害の発覚は本日**） |
| S-2 | `docs/readme.en.md` の陳腐化 | 英語圏読者に実 API が案内されない公開ドキュメント | **約 3 か月**（最終更新 2026-04-30） |

### 層 A — 運用が静かに壊れる／投資が回収できていない

| # | 項目 | リスク |
| --- | --- | --- |
| A-1 | AIHints 再同期が**リポジトリ全体の `npm test`** に依存 | `develop` の赤を 1 件放置するだけで、`addon-ai-tag` の自動 PR が**無言で止まる**。判明済みの失敗 2 件はどちらもこれ |
| A-2 | `ColorPalette` → `palette_priority` の導出が未実装 | 94 件の配色投入と抽出ツール実装の成果が AIHints へ届かない。**繋ぐ 1 本だけが欠けている** |
| A-3 | `develop` → `addon-ai-tag` 未マージ 1 件 | 今回は進捗ログのみで実害なし。ただし**気づく仕組みが無い**のが本質的な問題 |

### 層 B — 技術負債・未検証

| # | 項目 | 補足 |
| --- | --- | --- |
| B-1 | `basicFields` 整列後の**実機目視が未実施** | 表示順を変えた変更が実ブラウザ未確認のまま **8 日**。後続 UI 作業の前提 |
| B-2 | field-order Phase 6（UI/SW のマージ順が逆） | 既知の実装不整合。放置するほど後続の UI 変更が乗りにくい |
| B-3 | field-order Phase 4（ネスト整列） | `Images` の子キー 26 通り → 1 通り。表示に効かない範囲に限定可能 |
| B-4 | `$VersDef` / `$VarsDef` 表記ゆれ | 4 作品分が辞書合成経路から漏れている。実害は未観測だが要裏取り |
| B-5 | Worker `/works` への `OfficialLinks` 明示追加 | 2026-07-22 に本番実測で未対応を確認済み |
| B-6 | Workers 側 `_Secondaries` マッチャの乖離 | 現行データでは実害なし。`sec_Category` で分岐させると Workers だけ挙動が変わる |
| B-7 | `calling.js` のユニットテスト / UI 確認 | 2026-06-24 から **1 か月**未着手 |
| B-8 | ICS の Drive ミラー再アップロード / アーティファクト確認 | Git 作業ツリー外・User 環境での操作 |

### 層 C — User の入力・判断待ち（Claude 側は完了済み）

滞留日数は 2026-07-25 時点。**放置しても壊れないが、長いものは「畳むか進めるか」の判断が要る**。

| 項目 | 滞留 | 備考 |
| --- | --- | --- |
| Issue #13 のフィールド設計判断 | 4 日 | 外部（Bot 側）からの依頼。Bot 側はフォールバックするため緊急度は低い |
| ColorPalette の `Role` レビュー / `ColorName_*` 入力 | 12 日 | **A-2 の前提**になる。`Role` だけでも確定できると進みやすい |
| AppearanceDetail `10`/`10alt` 確認・保留 4 枚 | 14 日 | — |
| UnibyteLive 苗字 24+2 件のレビュー | 19 日 | I・O の2代目は基礎情報未設定・配置も保留 |
| Localization Summary の未入力 7 件 | 30 日 | 地名 4 / 人物名 2 / FL78 現象・能力ほか |
| ConversationPattern（Num 92/94/95/98/99/2/10） | 15 日 | **User の `DialogueExamples` 先行入力が起点**。sub2 の stale lock 解消も User 端末作業 |

### 層 D — 長期保留（着手判断そのものが保留）

| 項目 | 状況 |
| --- | --- |
| ADR-0002（Google Cloud 画像生成） | Draft のまま **約 1 か月**。次アクション 4 件が未着手。別リポジトリとの調整が前提 |
| `addon-ai-tag` 逆マージ事故の後日談追記 | `addon-ai-tag` チェックアウト環境が要る。再発防止策の検討は User 判断待ち |
| ロールプレイプロンプト EN 版（フェーズ4） | 着手前調査は完了。**出力先形式と呼称 EN 表記の User 確認**が着手条件 |
| 創作用語DB / 基本資料DB | 最小テンプレート案の作成から。辞書本文は User 手動入力前提 |

---

## 4. 今後の対応方針（明文化）

### 4-1. 着手順の推奨

台帳 `2026-07-03_current-task-ledger.md` の冒頭にも同じ表を置いた。**上ほど先に片付ける**。

| 順 | 対象 | 判断理由 | 参照 |
| --- | --- | --- | --- |
| **1** | ロールプレイプロンプトの体裁 3 件 | **配布物に実害が出ている唯一の項目**。再生成では直らない | 母艦 P4-8 |
| **2** | `docs/readme.en.md` の更新 | 公開ドキュメントの陳腐化。放置期間が最長（約 3 か月） | 母艦 P4-9 |
| **3** | `basicFields` 整列後の実機目視 | 未検証の表示変更が**後続 UI 作業すべての前提**になる | 母艦 P4-10 |
| **4** | AIHints `--apply-colorpalette` の実装 | 94 件の投資が回収されていない。**欠けているのは 1 本だけ** | 母艦 P7-1 |
| **5** | field-order Phase 6（UI/SW マージ順の統一） | 既知の実装不整合。単独 PR で完結する | 母艦 P4-10 |
| **6** | Worker `/works` への `OfficialLinks` 明示追加 | 実害は無いが、対応差が残り続ける | 母艦 P4-6 |

**この順にした理由**: 「外に出ているものの実害 → 未検証の変更 → 回収されていない投資 → 内部の不整合」
という順序で並べた。層 C（User 待ち）は Claude 側から動かせないため順序に含めず、並行で進める。
層 D は着手判断そのものが保留のため、User から指示があったときに扱う。

### 4-2. 種類別の対応方針

| 種類 | 方針 |
| --- | --- |
| **配布物・公開物の不具合**（層 S） | 発見したら「User 判断待ち」で据え置かず、**まず影響範囲を実測**して母艦へ登録する。文面が変わる修正は User 確認を取るが、確認を待つ間も**実害の記録は残す** |
| **CI・自動化の失敗**（層 A） | 推定原因で対応を提案しない。**必ず Actions 実行ログを確認してから**方針を出す（2026-07-22 / 07-25 とも仮説を誤った実績がある） |
| **技術負債**（層 B） | 単独 PR で完結する粒度に割ってから着手する。UI に効く変更（Phase 6）と効かない変更（Phase 4）を混ぜない |
| **User 入力待ち**（層 C） | Claude 側からは催促せず、**着手順の前提になっているものだけ**を台帳で可視化する（例: ColorPalette の `Role` は A-2 の前提） |
| **長期保留**（層 D） | 棚卸しのたびに「まだ保留でよいか」だけを確認し、内容には立ち入らない |

### 4-3. 棚卸しのたびに実行する定点観測

今回、**書面と実態のズレが 3 件**、**同じ誤った仮説の再掲が 1 件**見つかった。
これらは以下を毎回実行していれば機械的に検出できる。台帳の運用メモにも追記した。

```bash
npm test                                                        # 赤があると addon-ai-tag の自動 PR が止まる
npm run agents:check                                            # 指示書の生成物ズレ
npm run data:order:check                                        # データのキー順ズレ
git rev-list --left-right --count develop...origin/addon-ai-tag # 一方向マージの未実施分
gh run list --limit 12                                          # CI の実状態（推測で代用しない）
gh issue list --state open                                      # 未解決 Issue
```

さらに、**ログに「未コミット」と書かれていたら `git log -- <該当ファイル>` で必ず裏取りする**。
今回の 3 件はすべてこれで解決した。

### 4-4. ログを書くときの申し送り

- **「User 判断待ち」は実害の有無とセットで書く**。今回の `[object Object]` は「判断待ち」として
  据え置かれていたが、実際には配布物 10 件に出ていた。判断待ちにするかどうかは、
  実害を測ってから決める。
- **自動生成されたトリアージログは仮説として扱う**。実行ログ未接続の推論であることが本文に
  書かれていても、次のセッションが「調査済みの結論」と読み違える。訂正が入ったら
  **元のログ側にも注記を残す**（今回そうした）。

---

## 5. 影響範囲（編集ファイル）

**進捗ログ・台帳**

- `_work_in_progress/README.md`（トピック索引の更新・退避一覧・整理履歴）
- `_work_in_progress/2026-07-08_remaining-task.md`（母艦。**P4 に 7 項目追加 → P4-14 / P7 を新設**）
- `_work_in_progress/2026-07-03_current-task-ledger.md`（台帳。**着手順の推奨**・滞留日数・定点観測を追加）
- `_work_in_progress/2026-07-25_github-triage.md`（**§1 の誤った仮説へ訂正注記**・§2/§3 の確定・まとめ表を追加）
- `_work_in_progress/2026-07-24_progress_roleplay-prompt-formatting.md`（**実測結果と記述の訂正**を追記）
- `_work_in_progress/2026-07-25_progress_wip-tidy.md`（本ファイル・新規）
- 退避した 4 件（`.completed/` へ移動、Git 管轄外。4 件とも確認結果を追記済み）

`pages/` `lib/` `data/` `tests/` への変更は**なし**。

## 6. 検証

- `npm test`: **42 ファイル / 582 件すべて成功**（棚卸し前後で変化なし）。
- `npm run agents:check`: `0/2 件が要更新`。`npm run data:order:check`: `0/1284 レコード`。
- 生成物の走査: `data/Works_*/RoleplayPrompts/` 全 66 件を grep し、`[object Object]` 10 件・
  句点二重化 1 件・文断裂 1 件の所在を特定。
- CI: `gh run list` で AIHints（`30141803427`）/ Cloudflare（`30141699982`・`30141803428`）/
  Pages（`30143189878`）の直近 run がすべて success であることを確認（読み取りのみ）。
- Issue: `gh issue list` で OPEN が **#13 のみ**であることを確認。
- コミット状態: `git rev-list --left-right --count` で `origin/develop` と 0/0、
  `origin/addon-ai-tag` と 1/108 を確認。

## 7. 未完了タスク

- **本棚卸しの成果は未コミット**（User の指示待ち）。
- 層 S / A / B の各項目は母艦 `2026-07-08_remaining-task.md` へ登録済み（本棚卸しでは実装しない）。

## 8. 申し送り事項

1. **母艦 P4-8（ロールプレイプロンプトの体裁）を最優先で**。配布物に出ている唯一の実害で、
   `render.mjs` の `resolvePath()` に共通アンラップを入れるのが筋だが**全フィールドへ効く**ため影響確認が要る。
   着手時は文面が変わる点について User 確認を取る。
2. **母艦 P4-12（AIHints 再同期の `npm test` 全体依存）は仕組みの問題**。
   `develop` で辞書構造を変えるときは、`addon-ai-tag` 側の限定テストが黙って壊れうる。
   マージ前に `addon-ai-tag` で `npm test` を通すのが唯一の予防策。
3. **母艦 P7-1（`--apply-colorpalette`）は「あと 1 本」の状態**。着手前に層 C の
   ColorPalette `Role` レビューが済んでいると手戻りが減る。
4. `addon-ai-tag` 側の進行中ログ（`aihints-*` 2 件 / `addon-ai-tag-reverse-merge-incident`）は、
   `addon-ai-tag` パスでの棚卸し対象として引き続き残置。
5. `develop` → `addon-ai-tag` の未マージ 1 件（`b737891`・進捗ログのみ）は、
   次に `addon-ai-tag` を触るときにまとめて取り込めばよい。

## 参考

- `_work_in_progress/README.md`（トピック別索引・退避一覧・整理履歴）
- `.completed/2026-07-22_progress_wip-tidy.md`（前回の棚卸し）
- `2026-07-25_progress_aihints-resync-ci-failure.md`（当日の CI 失敗調査。トリアージ §1 訂正の根拠）
- `CLAUDE.md` / `AGENTS.md`（`_work_in_progress/` の運用ルール・ブランチ運用方針・データ更新時のテスト追従）
